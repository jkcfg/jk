import { log } from 'std_log';
import { Format, write } from 'std_write';
import { Encoding, read } from 'std_read';
import { info, dir } from 'std_fs';
import { param } from 'std_param';
import { mix, patch } from 'std_merge';

export default {
  log,
  Format,
  write,
  Encoding,
  read,
  info,
  dir,
  param,
};
